name: Full Tunnel (Zrok)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tunnel:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Start mhrv-tunnel-node
        run: |
          docker run -d --name mhrv-tunnel \
            -p 8080:8080 \
            -e TUNNEL_AUTH_KEY="${{ secrets.TUNNEL_AUTH_KEY }}" \
            ghcr.io/therealaleph/mhrv-tunnel-node:latest
          sleep 5
          curl -s http://localhost:8080/health || sleep 5


      - name: Install WireGuard
        run: |
          sudo apt-get update
          sudo apt-get install -y wireguard-tools


      - name: Create tunnel (official command adapted)
        run: |
          curl https://tunnel.pyjam.as/8080 -o tunnel.conf
          sudo wg-quick up ./tunnel.conf
          sleep 10000

      - name: Cleanup
        if: always()
        run: |
          docker stop mhrv-tunnel || true
          wg-quick down ./tunnel.conf
